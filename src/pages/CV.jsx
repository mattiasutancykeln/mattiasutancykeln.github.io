/** CV page — placeholder until step 11 */

function CV() {
  return (
    <div style={{ padding: '2rem' }}>
      <p className="font-mono" style={{ color: 'var(--text-muted)' }}>
        ← home
      </p>
      <h1 className="font-display">CV</h1>
      <p>Placeholder — step 11.</p>
    </div>
  );
}

export default CV;
